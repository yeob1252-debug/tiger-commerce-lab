# 사장님 대시보드 백엔드 배포 가이드

이 문서 하나만 보고 그대로 따라하시면 됩니다. 두 단계입니다.
**1) 구글시트에 탭 만들기 → 2) Apps Script 붙여넣고 배포하기**

---

## 1단계. 구글시트에 탭(시트) 만들기

스프레드시트: `https://docs.google.com/spreadsheets/d/136ndDU2fJfoLP3w39Pxv4yoACGrzmVKNJ49mu0myf8g`

이 스프레드시트 안에 아래 6개 탭을 정확히 이 이름으로 만들어주세요 (이미 있는 탭은 이름/헤더만 아래와 맞으면 됩니다).

### ① 대시보드 (key-value 2열)
| 항목 | 값 |
|---|---|
| 계약시작일 | 2026-01-01 |
| 계약종료일 | 2026-12-31 |
| 총영업외추가매출액 | 12000000 |
| 유튜브틱톡누적조회수 | 450000 |
| 단골가두기방회원수 | 320 |
| 현재단계 | 2 |
| 1단계명 | 메뉴 브랜딩 |
| 1단계상태 | 완료 |
| 2단계명 | SNS 채널 개설 |
| 2단계상태 | 진행중 |
| 3단계명 | 콘텐츠 확장 |
| 3단계상태 | 예정 |
| 4단계명 | 라이브커머스 |
| 4단계상태 | 예정 |

- `1~4단계상태`는 `완료` / `진행중` / `예정` 중 하나로만 입력하세요.
- 계약시작일/종료일은 `YYYY-MM-DD` 형식.

### ② 월간성과 (표 형식, 첫 행이 헤더)
| 월 | 조회수 | 매출 | ROAS |
|---|---|---|---|
| 2026-01 | 12000 | 3200000 | 2.1 |
| 2026-02 | 18500 | 4100000 | 2.6 |

### ③ 운영캘린더
| 주차 | 시작일 | 콘텐츠 | 설명 | 상태 |
|---|---|---|---|---|
| 1주차 | 2026-07-28 | 여름 특선 숏폼 | 냉면 메뉴 후킹 콘텐츠 | 진행중 |

- `시작일`이 이번 주(오늘 기준 월~일)에 속하면 대시보드에서 자동으로 강조됩니다.

### ④ 메뉴단가
| 메뉴명 | 가격 | 비고 |
|---|---|---|
| 간장게장 정식 | 32000 | 2인 기준 |

### ⑤ 소통게시판
| 날짜 | 작성자 | 내용 |
|---|---|---|

- 이 탭은 처음엔 헤더만 있으면 됩니다. 사장님이 대시보드에서 댓글을 남기면 자동으로 이 탭에 새 행이 추가됩니다.

### ⑥ 고객목록 (매장 여러 곳을 관리하기 위한 마스터 탭 — 이 스프레드시트에만 필요)
| slug | 매장명 | 구글시트ID | 로그인비밀번호 |
|---|---|---|---|
| jangjinyeong | 장지녕간장게장 | 136ndDU2fJfoLP3w39Pxv4yoACGrzmVKNJ49mu0myf8g | 여기에원하는비밀번호 |

- `구글시트ID`는 스프레드시트 URL에서 `/d/`와 `/edit` 사이 부분입니다. 지금은 첫 매장 데이터가 이 시트 자체에 있으니 위 표처럼 **같은 스프레드시트 ID**를 넣으면 됩니다.
- 매장이 새로 추가되면: 이 스프레드시트를 "사본 만들기"로 복제해서 새 매장용 5개 탭(①~⑤)을 채우고, 그 사본의 ID를 `고객목록`에 새 행으로 추가하면 끝입니다. (마스터 시트에는 `고객목록` 탭만 있으면 되고, 나머지 5개 탭은 각 매장 사본에 있으면 됩니다.)
- 비밀번호는 이 탭에만 저장되고, 대시보드 화면에는 절대 노출되지 않습니다.

---

## 2단계. Apps Script 붙여넣고 배포하기

1. 위 스프레드시트를 엽니다.
2. 상단 메뉴 **확장 프로그램 → Apps Script** 클릭.
3. 열린 편집기에서 기본으로 있는 `Code.gs` 내용을 전부 지우고, 이 저장소의 `.claude/apps-script/Code.gs` 파일 내용을 그대로 복사해서 붙여넣습니다.
4. 저장 (Ctrl+S 또는 디스크 아이콘).
5. 우측 상단 **배포 → 새 배포** 클릭.
6. 유형 선택에서 톱니바퀴 아이콘 클릭 → **웹 앱** 선택.
7. 설정:
   - **다음 사용자 권한으로 실행**: 나 (내 계정)
   - **액세스 권한이 있는 사용자**: 전체
8. **배포** 클릭 → 처음이면 Google이 권한 승인을 요청합니다. 본인 계정으로 로그인 → "고급" → "안전하지 않음(프로젝트명)으로 이동" → 허용까지 진행합니다.
9. 배포가 끝나면 **웹 앱 URL**이 나옵니다 (`https://script.google.com/macros/s/...../exec` 형태). 이 URL을 복사해서 저에게 알려주세요.

이후 제가 그 URL을 사이트 코드(`client.html`, `login.html`, `admin.html`)의 `APPS_SCRIPT_URL` 값에 넣고 배포하면 실제 데이터 연동이 완료됩니다.

> 시트 내용을 수정하고 나서 스크립트 코드 자체를 바꾼 경우에만 "배포 → 배포 관리 → 수정 → 새 버전"으로 다시 배포하면 됩니다. 시트의 값만 바꾸는 건(비밀번호, 매출 숫자 등) 다시 배포할 필요 없이 바로 반영됩니다.
